image: rustlang/rust:nightly

pipelines:
  default:
    - step:
        name: "Building Blackbody"
        script:
          - apt update
          - apt install -y libgtk-3-dev
          - cargo build --release